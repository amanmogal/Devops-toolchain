apiVersion: v1
kind: Service
metadata:
  name: prometheus-pushgateway
  namespace: monitoring
spec:
  ports:
  - port: 9091
    targetPort: 9091
  selector:
    app: prometheus-pushgateway
  type: LoadBalancer 
